# JSON可視化ツール - 実装準備完了確認書

## 📅 日時: 2025-11-10

## ✅ 実装に必要な情報が完全に揃いました

---

## 🎯 プロジェクト要約

### コンセプト
JSONデータをカードと線で可視化する、家系図スタイルのビジュアライゼーションツール

### 配置場所
`src/pages/JSONVisualization.vue`

### 技術スタック
- **既存**: Vue 3, Vue Router, Tailwind CSS, js-yaml
- **新規**: Vue Flow (`@vue-flow/core`, `@vue-flow/background`, `@vue-flow/controls`)

---

## 📋 確定仕様一覧

### 1. データ変換ルール

#### ✅ すべてのキー・バリューを個別カード化
```json
{
  "user": {
    "name": "太郎",
    "age": 25
  }
}
```
↓ 変換 ↓
```
- root (object, 1 keys)
- user (object, 2 keys)
- name (string, "太郎")
- age (number, 25)
```
**合計: 4枚のカード**

#### ✅ 配列の扱い
```json
["apple", "banana", "cherry"]
```
↓ 変換 ↓
```
- 配列自体 (array, 3 items)
- [0] (string, "apple")
- [1] (string, "banana")
- [2] (string, "cherry")
```
**合計: 4枚のカード** (配列 + 要素3つ)

#### ✅ 配列内オブジェクト
```json
{
  "users": [
    {"name": "太郎", "age": 25},
    {"name": "花子", "age": 23}
  ]
}
```
↓ 変換 ↓
```
root (object, 1 keys)
  └─ users (array, 2 items)
       ├─ [0] (object, 2 keys)
       │    ├─ name (string, "太郎")
       │    └─ age (number, 25)
       └─ [1] (object, 2 keys)
            ├─ name (string, "花子")
            └─ age (number, 23)
```
**合計: 9枚のカード**

#### ✅ ルートオブジェクトの扱い
- **常に "root" カードを表示**
- 単一キーでも省略しない
- ノードID: `"root"`

---

### 2. カードデザイン仕様

#### プリミティブ値カード
```
┌─────────────────┐
│  name           │ ← キー (太字)
│  ─────────────  │
│  "太郎"         │ ← 値
│  [string]       │ ← データ型
└─────────────────┘
```

#### オブジェクト/配列カード
```
┌─────────────────┐
│  user           │ ← キー (太字)
│  ─────────────  │
│  { ... }        │ ← 視覚表現
│  [object]       │ ← データ型
│  2 keys         │ ← 子要素数
└─────────────────┘
```

#### データ型別カラー (Tailwind)
| データ型 | 背景色 | クラス |
|---------|-------|--------|
| string | 青 | `bg-blue-600` |
| number | 緑 | `bg-green-600` |
| boolean | 黄 | `bg-yellow-600` |
| null | グレー | `bg-gray-600` |
| array | 紫 | `bg-purple-600` |
| object | ピンク | `bg-pink-600` |

---

### 3. レイアウト仕様

#### 家系図スタイル (縦方向階層レイアウト)
```
           [root]
              |
           [user]
              |
         ┌────┴────┐
      [name]    [age]
```

#### 配置計算ルール
- **Y軸**: `depth × 150px` (深さごとに下へ)
- **X軸**: 兄弟ノードを水平に均等配置
- **文字方向**: 左から右 (横書き)

#### 配置アルゴリズム例
```javascript
function calculatePosition(depth, siblingIndex, siblingCount) {
  const y = depth * 150;
  const xSpacing = 200;
  const xOffset = (siblingIndex - (siblingCount - 1) / 2) * xSpacing;
  return { x: xOffset, y };
}
```

---

### 4. 接続線仕様

- **方向**: 親 → 子 (下向き矢印)
- **スタイル**: シンプルな実線
- **色**: `stroke-gray-400` (統一)
- **太さ**: `stroke-width: 2`

---

### 5. 文字列の長さ制限

#### ✅ v1.0 では制限なし
- カードが自動的に拡大
- 将来的にホバー機能やスクロール対応を検討

---

### 6. MVP機能スコープ

#### ✅ 実装する機能
- [x] JSON入力 (textarea)
- [x] カード自動生成 (すべてのキー・バリュー)
- [x] 階層構造の線表示
- [x] ドラッグ&ドロップ (カード移動)
- [x] ズーム機能
- [x] パン機能 (キャンバス移動)
- [x] サンプルJSON読み込み
- [x] クリアボタン
- [x] エラーハンドリング (JSON構文エラー)

#### ❌ v1.0 では実装しない
- [ ] ノードの折りたたみ/展開
- [ ] カード上での値編集
- [ ] エクスポート機能 (PNG/SVG)
- [ ] 検索・フィルタリング
- [ ] 複数JSONの比較
- [ ] ダークモード切り替え
- [ ] ファイルアップロード

---

## 🗂️ ファイル構成

```
src/
├── pages/
│   └── JSONVisualization.vue      # メインページ (新規作成)
├── components/
│   └── json-viz/
│       ├── PrimitiveCard.vue      # プリミティブ値カード (新規)
│       ├── ObjectCard.vue         # オブジェクト/配列カード (新規)
│       └── JSONParser.js          # 変換ロジック (新規)
└── router/
    └── index.js                    # ルート追加 (編集)
```

---

## 🔧 実装詳細仕様

### JSONParser.js の責務

#### 入力
```javascript
const jsonString = '{"user": {"name": "太郎", "age": 25}}';
```

#### 出力
```javascript
{
  nodes: [
    {
      id: "root",
      type: "objectCard",
      data: { key: "root", value: null, dataType: "object", childCount: 1 },
      position: { x: 0, y: 0 }
    },
    {
      id: "root.user",
      type: "objectCard",
      data: { key: "user", value: null, dataType: "object", childCount: 2 },
      position: { x: 0, y: 150 }
    },
    {
      id: "root.user.name",
      type: "primitiveCard",
      data: { key: "name", value: "太郎", dataType: "string" },
      position: { x: -100, y: 300 }
    },
    {
      id: "root.user.age",
      type: "primitiveCard",
      data: { key: "age", value: 25, dataType: "number" },
      position: { x: 100, y: 300 }
    }
  ],
  edges: [
    { id: "e-root-user", source: "root", target: "root.user" },
    { id: "e-user-name", source: "root.user", target: "root.user.name" },
    { id: "e-user-age", source: "root.user", target: "root.user.age" }
  ]
}
```

#### 処理フロー
1. JSON文字列をパース (`JSON.parse()`)
2. ルートノード生成 (id: "root")
3. 再帰的にすべての子要素を処理
4. 配列は `[index]` キーで展開
5. 各ノードの位置を計算 (家系図レイアウト)
6. 親子関係からエッジを生成

---

### PrimitiveCard.vue の仕様

#### Props
```javascript
{
  data: {
    key: String,        // "name"
    value: Any,         // "太郎"
    dataType: String    // "string"
  }
}
```

#### テンプレート構造
```vue
<div class="card" :class="typeColor">
  <div class="key">{{ data.key }}</div>
  <hr>
  <div class="value">{{ formatValue(data.value) }}</div>
  <div class="type">[{{ data.dataType }}]</div>
</div>
```

#### スタイリング
- Tailwind CSS使用
- 幅: 自動 (内容に応じて)
- パディング: `p-4`
- 角丸: `rounded-lg`
- シャドウ: `shadow-md`
- テキスト: `text-white`

---

### ObjectCard.vue の仕様

#### Props
```javascript
{
  data: {
    key: String,        // "user"
    value: null,        // オブジェクト/配列は値を持たない
    dataType: String,   // "object" or "array"
    childCount: Number  // 2
  }
}
```

#### テンプレート構造
```vue
<div class="card" :class="typeColor">
  <div class="key">{{ data.key }}</div>
  <hr>
  <div class="symbol">{{ typeSymbol }}</div>
  <div class="type">[{{ data.dataType }}]</div>
  <div class="count">{{ data.childCount }} {{ countLabel }}</div>
</div>
```

#### 表示記号
- object: `{ ... }`
- array: `[ ... ]`

#### カウントラベル
- object: `keys`
- array: `items`

---

### JSONVisualization.vue の構造

#### Data
```javascript
{
  jsonInput: "",      // テキストエリアの入力
  nodes: [],          // Vue Flow ノード
  edges: [],          // Vue Flow エッジ
  errorMessage: ""    // エラーメッセージ
}
```

#### Methods
```javascript
{
  visualizeJSON() {
    // 1. JSON検証
    // 2. JSONParser呼び出し
    // 3. nodes, edges更新
    // 4. エラーハンドリング
  },

  loadSample() {
    // サンプルJSONをロード
  },

  clearCanvas() {
    // 入力とキャンバスをクリア
  }
}
```

---

## 🚀 実装手順 (5フェーズ)

### Phase 1: 環境セットアップ ✅
```bash
npm install @vue-flow/core @vue-flow/background @vue-flow/controls
```
- ルーター設定更新
- 基本ページファイル作成

### Phase 2: JSONパーサー実装 ✅
- `src/components/json-viz/JSONParser.js` 作成
- データ型判定ロジック
- 再帰的ノード生成
- 配列処理 (インデックスキー)
- レイアウト計算 (家系図)
- エッジ生成

### Phase 3: カードコンポーネント ✅
- `src/components/json-viz/PrimitiveCard.vue`
- `src/components/json-viz/ObjectCard.vue`
- データ型別スタイリング
- Tailwind統合

### Phase 4: メインページ実装 ✅
- `src/pages/JSONVisualization.vue`
- Vue Flow統合
- UI構築 (入力エリア + キャンバス)
- イベントハンドリング
- エラー表示

### Phase 5: 最終調整 ✅
- スタイル調整
- 動作テスト
- エラーケース検証
- ドキュメント更新

---

## 📊 サンプルJSONデータ

### シンプルな例
```json
{
  "name": "太郎",
  "age": 25,
  "active": true
}
```
**期待結果**: 4枚のカード (root + 3フィールド)

### 配列を含む例
```json
{
  "user": "太郎",
  "hobbies": ["読書", "映画", "音楽"]
}
```
**期待結果**: 7枚のカード (root, user, hobbies, 3要素)

### ネストしたオブジェクト
```json
{
  "user": {
    "profile": {
      "name": "太郎",
      "age": 25
    },
    "settings": {
      "theme": "dark"
    }
  }
}
```
**期待結果**: 8枚のカード

### 配列内オブジェクト
```json
{
  "users": [
    {"name": "太郎", "age": 25},
    {"name": "花子", "age": 23}
  ]
}
```
**期待結果**: 9枚のカード

---

## ✅ 実装準備チェックリスト

### 仕様の明確性
- [x] データ変換ルールが明確
- [x] カードデザインが確定
- [x] レイアウトアルゴリズムが定義済み
- [x] 配列の扱いが決定
- [x] ルートオブジェクトの扱いが決定
- [x] 文字列制限の方針が決定

### 技術的準備
- [x] 使用ライブラリが決定 (Vue Flow)
- [x] ファイル構成が明確
- [x] コンポーネント責務が定義済み
- [x] データフロー設計が完了

### 実装スコープ
- [x] MVP機能が明確
- [x] 実装しない機能が明確
- [x] 実装フェーズが定義済み

---

## 🎉 結論

**すべての実装情報が揃いました！**

次のステップ:
1. `npm install @vue-flow/core @vue-flow/background @vue-flow/controls`
2. Phase 1 から順次実装開始

---

**作成日**: 2025-11-10
**ステータス**: ✅ 実装準備完了
**承認**: ユーザー確認済み
